<%= form_with(model: post2, local: true) do |form| %>
  <% if post2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post2.errors.count, "error") %> prohibited this post2 from being saved:</h2>

      <ul>
      <% post2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :post2_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :post2_body %>
  </div>

  <div class="field">
    <%= form.label :published %>
    <%= form.check_box :published, id: :post2_published %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
